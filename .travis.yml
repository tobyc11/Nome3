language: cpp
os: osx
osx_image: xcode11.3
compiler:
  - gcc
  - clang

addons:
  homebrew:
    packages:
      - qt
      - open-mesh
      - pugixml
      - bison
      - flex
      - cmake
      - antlr
    update: True


script:
 - cmake . && make
